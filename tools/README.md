# tools/

Scripts auxiliares que NO forman parte del sitio desplegado.

- `parse_efem.py` — script one-off que generó `efemerides.json` a partir del archivo
  fuente de efemérides. No se ejecuta en producción; queda como referencia de cómo se
  armó el dataset.
